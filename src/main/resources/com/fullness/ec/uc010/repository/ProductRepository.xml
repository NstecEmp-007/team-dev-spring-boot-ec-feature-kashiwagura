<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fullness.ec.UC010.repository.ProductRepository">
    <insert id="insert" parameterType="Product">
        INSERT INTO product (name, price, quantity AS stock, image AS imageURL, category_id AS categoryId)
        VALUES (#{name}, #{price}, #{stock}, #{image}, #{categoryId})
    </insert>
    </mapper>